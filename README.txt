1)copy tcet-welcome.desktop to /etc/xdg/autostart.

2)assumes the git repo is cloned to ~/.local/share (/home/[usr]/.local/share). 

3)The welcome app is made using yad and the 'about us' tab is made using python and GTK3.0 the 'about us' box is not resizeable by design.

4) yad isnt installed by default in TCET linux.

